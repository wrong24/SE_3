version: '3.8'
services:
  testing_tools:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8016-8020:8016-8020"
    networks:
      - lab_network

networks:
  lab_network:
    driver: bridge